/**
 * posizione-lavorativa-en controller
 */

import { factories } from "@strapi/strapi";

export default factories.createCoreController(
  "api::posizione-lavorativa-en.posizione-lavorativa-en",
);
